import { Clock3, MapPin, Search, SlidersHorizontal, UserCheck } from 'lucide-react'
import { Link } from 'react-router-dom'
import { candidates } from '../data/mockData'
import Badge from '../components/ui/Badge'
export default function CandidateManagement(){return <main className="p-8"><div className="flex items-start justify-between"><div><h1 className="page-title">Candidate Management</h1><p className="muted mt-1">Analyze and manage talent in your recruitment pipeline with AI-driven ranking.</p></div><button className="grad-accent rounded-lg px-5 py-2.5 text-sm font-semibold text-white">+ Add Candidate</button></div><div className="mt-7 flex items-center justify-between"><div className="rounded-xl border border-slate-200 bg-slate-100 p-1 text-sm font-medium"><button className="rounded-lg bg-white px-5 py-2 text-blue-600 shadow-sm">All Candidates (324)</button><button className="px-5 py-2 text-slate-500">Shortlisted (48)</button><button className="px-5 py-2 text-slate-500">Archived</button></div><div className="flex gap-3"><label className="flex items-center gap-2 rounded-xl border border-slate-200 bg-white px-4"><Search className="size-4 text-slate-400"/><input className="w-40 py-2.5 text-sm outline-none" placeholder="Search"/></label><button className="rounded-xl border border-slate-200 bg-white p-3"><SlidersHorizontal className="size-4"/></button></div></div><div className="mt-7 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{candidates.map(c=><Link to={`/candidates/${c.id}`} key={c.id} className="card p-5 transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-center gap-3"><div className="grid size-12 place-items-center rounded-full bg-gradient-to-br from-blue-100 to-violet-100 font-bold text-blue-700">{c.name.split(' ').map(n=>n[0]).join('')}</div><div className="min-w-0"><h2 className="font-bold">{c.name}</h2><p className="truncate text-sm text-slate-500">{c.role}</p></div></div><div className="mt-4 flex gap-4 text-xs text-slate-500"><span className="flex items-center gap-1"><MapPin className="size-3"/>{c.location}</span><span className="flex items-center gap-1"><Clock3 className="size-3"/>{c.experience}</span></div><div className="mt-5 rounded-xl bg-blue-50 p-4"><div className="flex items-center justify-between text-sm font-semibold text-blue-700"><span>AI MATCH SCORE</span><span>{c.score}%</span></div></div><p className="mb-2 mt-5 text-xs text-slate-500">Top Skills</p><div className="flex flex-wrap gap-1.5">{c.skills.map(s=><Badge key={s} className="bg-violet-50 text-violet-600">{s}</Badge>)}</div><button className="mt-5 flex w-full items-center justify-center gap-2 rounded-lg border border-slate-200 py-2 text-sm font-semibold"><UserCheck className="size-4 text-emerald-500"/>Shortlist</button></Link>)}</div></main>}
