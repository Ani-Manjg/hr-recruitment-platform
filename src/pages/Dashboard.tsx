import { BrainCircuit, CalendarDays } from 'lucide-react'
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { dashboardStats, matchCriteria, pipelineHistory } from '../data/mockData'
import StatCard from '../components/ui/StatCard'

export default function Dashboard(){return <main className="p-8"><section className="card relative overflow-hidden bg-gradient-to-r from-white to-blue-50 px-9 py-8"><div className="absolute -right-16 -top-16 size-60 rounded-full bg-violet-100/30 blur-2xl"/><div className="relative"><h1 className="text-3xl font-bold tracking-tight">Welcome back, TalentFlow Admin</h1><p className="mt-2 max-w-3xl text-slate-500">Your AI recruitment agent has processed <strong className="text-blue-600">48 new candidates</strong> since your last login. View the latest high-match scores below.</p><div className="mt-5 flex gap-3"><button className="grad-accent flex items-center gap-2 rounded-lg px-5 py-2.5 text-sm font-semibold text-white shadow-sm"><BrainCircuit className="size-4"/>Run AI Analysis</button><button className="flex items-center gap-2 rounded-lg border border-slate-200 bg-white px-5 py-2.5 text-sm font-semibold"><CalendarDays className="size-4"/>Schedule Interviews</button></div></div></section><section className="my-9 grid grid-cols-2 divide-x divide-slate-100 lg:grid-cols-4">{dashboardStats.map(stat=><StatCard key={stat.label} stat={stat}/>)}</section><section className="grid gap-6 xl:grid-cols-[2fr_1fr]"><div className="card p-7"><div className="mb-6 flex items-start justify-between"><div><h2 className="text-xl font-bold">Recruitment Pipeline</h2><p className="muted">Historical view of candidate progression</p></div><span className="text-xs font-medium text-slate-500">Last 6 Months</span></div><div className="h-72"><ResponsiveContainer width="100%" height="100%"><AreaChart data={pipelineHistory} margin={{top:10,right:10,left:-20,bottom:0}}><defs><linearGradient id="screened" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#2563eb" stopOpacity={.2}/><stop offset="100%" stopColor="#2563eb" stopOpacity={0}/></linearGradient><linearGradient id="shortlisted" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#7c3aed" stopOpacity={.18}/><stop offset="100%" stopColor="#7c3aed" stopOpacity={0}/></linearGradient></defs><CartesianGrid stroke="#e8ebf0" strokeDasharray="3 3" vertical={false}/><XAxis dataKey="month" tickLine={false} axisLine={false} tick={{fill:'#7b8493',fontSize:12}}/><YAxis domain={[0,100]} tickLine={false} axisLine={false} tick={{fill:'#7b8493',fontSize:12}}/><Tooltip contentStyle={{borderRadius:12,border:'1px solid #e2e8f0'}}/><Area type="monotone" dataKey="screened" stroke="#2563eb" strokeWidth={2.5} fill="url(#screened)"/><Area type="monotone" dataKey="shortlisted" stroke="#7c3aed" strokeWidth={2.5} fill="url(#shortlisted)"/></AreaChart></ResponsiveContainer></div></div><div className="card p-7"><h2 className="text-xl font-bold">AI Match Criteria</h2><p className="muted mb-8">Weighted analysis of top candidates</p><div className="space-y-6">{matchCriteria.map(item=><div key={item.label}><div className="mb-2 flex justify-between text-sm font-medium"><span>{item.label}</span><span className="text-blue-600">{item.value}%</span></div><div className="h-1.5 overflow-hidden rounded-full bg-slate-100"><div className="grad-accent h-full rounded-full" style={{width:`${item.value}%`}}/></div></div>)}</div></div></section></main>}
