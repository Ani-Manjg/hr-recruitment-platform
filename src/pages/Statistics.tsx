import { ArrowUpRight, Clock3, Target, Users } from 'lucide-react'
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { pipelineHistory } from '../data/mockData'

const hiring=[{label:'Applications',value:'1,284',change:'+12%',icon:Users},{label:'Avg. Match Score',value:'78%',change:'+5%',icon:Target},{label:'Time to Hire',value:'18 days',change:'-2 days',icon:Clock3}]

export default function Statistics(){return <main className="p-8"><div><h1 className="page-title">Recruitment Statistics</h1><p className="muted mt-1">Track talent acquisition performance and hiring efficiency.</p></div><section className="mt-7 grid gap-5 md:grid-cols-3">{hiring.map(({label,value,change,icon:Icon})=><div className="card p-6" key={label}><div className="flex items-center justify-between"><div className="grid size-10 place-items-center rounded-xl bg-blue-50 text-blue-600"><Icon className="size-5"/></div><span className="flex items-center text-xs font-semibold text-emerald-600"><ArrowUpRight className="size-3"/>{change}</span></div><p className="mt-6 text-sm text-slate-500">{label}</p><p className="mt-1 text-2xl font-bold">{value}</p></div>)}</section><section className="card mt-6 p-7"><div><h2 className="text-xl font-bold">Annual Pipeline Performance</h2><p className="muted mt-1">Screened and shortlisted candidates by month</p></div><div className="mt-7 h-96"><ResponsiveContainer width="100%" height="100%"><BarChart data={pipelineHistory} barGap={4}><CartesianGrid stroke="#e8ebf0" strokeDasharray="3 3" vertical={false}/><XAxis dataKey="month" tickLine={false} axisLine={false} tick={{fill:'#7b8493',fontSize:12}}/><YAxis tickLine={false} axisLine={false} tick={{fill:'#7b8493',fontSize:12}}/><Tooltip cursor={{fill:'#f8fafc'}} contentStyle={{borderRadius:12,border:'1px solid #e2e8f0'}}/><Bar dataKey="screened" fill="#2563eb" radius={[5,5,0,0]}/><Bar dataKey="shortlisted" fill="#7c3aed" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div></section></main>}
